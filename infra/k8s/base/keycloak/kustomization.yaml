apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # CRDs
  - operator/crds-1.yaml
  - operator/crds-2.yaml
  # Operator
  - operator/operator.yaml
  # Keycloak
  - example-secrets.yaml
  - keycloak.yaml